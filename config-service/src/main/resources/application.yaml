spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          default-label: eureka
          uri: https://github.com/diegosg05/microservicios-course
          search-paths: config-data

server:
  port: 8084
